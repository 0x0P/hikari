# Example environment file for hikari.
# PostgreSQL connection string; adjust credentials if needed.
DATABASE_URL=postgresql://hikari:hikari@localhost:5432/hikari?schema=public

# Redis connection string.
REDIS_URL=redis://localhost:6379

# Optional: TTL for cache entries in seconds.
CACHE_TTL=120